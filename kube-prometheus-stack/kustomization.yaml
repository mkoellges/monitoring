apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-repo.yaml

namespace: monitoring

secretGenerator:
  - name: grafana-admin-credentials
    files:
      - admin-user
      - admin-password
